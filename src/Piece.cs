using static Chess.Game;

namespace Chess
{
    class Pawn
    {
        public static bool ValidMove(string input, Pieces[,] board, VectorPair coords)
        {
            return true;
        }
    }
}
